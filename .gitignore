# This .gitignore file should be placed at the root of your Unity project directory
#
# Get latest from https://github.com/github/gitignore/blob/main/Unity.gitignore
#
.utmp/
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
*.log

# MemoryCaptures can get excessive in size.
# They also could contain extremely sensitive data
/[Mm]emoryCaptures/

# Recordings can get excessive in size
/[Rr]ecordings/

# Uncomment this line if you wish to ignore the asset store tools plugin
# /[Aa]ssets/AssetStoreTools*

# Autogenerated Jetbrains Rider plugin
/[Aa]ssets/Plugins/Editor/JetBrains*

# Visual Studio cache directory
.vs/

# Gradle cache directory
.gradle/

# Autogenerated VS/MD/Consulo solution and project files
ExportedObj/
.consulo/
*.csproj
!Tools/ExternalAssetsLauncher/ExternalAssetsLauncher.csproj
!Tools/ExternalAssetsPublisher/ExternalAssetsPublisher.csproj
!Tools/ExternalAssetsBaseBuilder/ExternalAssetsBaseBuilder.csproj
!Tools/ExternalAssetsDeltaCommon/ExternalAssetsDeltaCommon.csproj
!Tools/ExternalAssetsContributor/ExternalAssetsContributor.csproj
!Tools/DbBackup/DbBackup.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Unity3D generated meta files
*.pidb.meta
*.pdb.meta
*.mdb.meta

# Unity3D generated file on crash reports
sysinfo.txt

# Builds
*.apk
*.aab
*.unitypackage
*.unitypackage.meta
*.app

# Crashlytics generated file
crashlytics-build.properties

# Packed Addressables
/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*

# Temporary auto-generated Android Assets
/[Aa]ssets/[Ss]treamingAssets/aa.meta
/[Aa]ssets/[Ss]treamingAssets/aa/*

# ===== 대형 외부 에셋 (산 에셋/외주/음원) =====
# 깃 대신 공유 드라이브로 배포. 내부 파일은 절대 커밋 금지.
# ExternalAssets.meta 하나만 Git으로 추적해 루트 폴더 GUID를 고정한다.
# 내부 에셋은 .meta를 포함해 Publisher로 압축하여 공유한다.
/[Aa]ssets/ExternalAssets/

# 외부 에셋 배포 산출물에는 실제 Drive 링크와 라이선스 에셋 ZIP이 포함된다.
# 반드시 제한된 저장소에서만 공유하고 공개 Git 저장소에는 절대 올리지 않는다.
/ExternalAssetsReleases/
/ExternalAssetsBaseBuilds/
/ExternalAssetsContributions/
/ExternalAssetsLegacyBackups/
/Tools/ExternalAssetsLauncher/google-oauth-client.json

# ===== Firebase 비-Windows 데스크톱 네이티브 (Windows 타깃이라 미사용, 대용량) =====
# macOS(.bundle 107MB)/Linux(.so 77MB) 데스크톱 라이브러리는 GitHub 100MB 제한 초과 + Windows에선 미사용.
# Windows는 x86_64/*.dll(17MB) 사용. Mac/Linux 개발 시 Firebase SDK를 각자 재임포트할 것.
/[Aa]ssets/[Ff]irebase/[Pp]lugins/x86_64/*.bundle
/[Aa]ssets/[Ff]irebase/[Pp]lugins/x86_64/*.bundle.meta
/[Aa]ssets/[Ff]irebase/[Pp]lugins/x86_64/*.so
/[Aa]ssets/[Ff]irebase/[Pp]lugins/x86_64/*.so.meta

# ===== Firebase CLI (standalone binary + logs) — 절대 커밋 금지 =====
# firebase.tools 단독 실행 파일(~240MB, Node 없이 규칙 배포용). firebase.json(배포 설정)은 커밋해도 됨.
/firebase.exe
firebase-tools-instant-win.exe
firebase-debug.log
.firebase/

# ===== DB 백업 도구 (Tools/DbBackup) — 실제 설정·키·백업본은 절대 커밋 금지 =====
# 설정은 예시(dbbackup.config.example.json)만 커밋한다. 서비스 계정 키는 DB 전체 읽기/쓰기 권한이라
# 저장소 밖에 두는 게 원칙이지만, 실수로 옮겨 놓아도 올라가지 않게 이름 패턴까지 막는다.
/Tools/DbBackup/dbbackup.config.json
/Tools/DbBackup/**/*-sa.json
# 서비스 계정 키 이름 패턴은 폴더와 무관하게 저장소 전체에서 막는다(키를 어디로 옮겨 놓든 올라가지 않게).
*serviceAccount*.json
*service-account*.json
# Firebase 콘솔 "새 비공개 키 생성" 파일 이름({프로젝트}-firebase-adminsdk-…json). 저장소 어디에 떨어져도 막는다.
*firebase-adminsdk*.json
/Tools/DbBackup/bin/
/Tools/DbBackup/obj/

# Unity scene recovery backups (crash recovery temp folder)
/[Aa]ssets/_Recovery/

# Local-only realtime bug log endpoint configuration
/Assets/Resources/Logging/LogSettings.local.asset
/Assets/Resources/Logging/LogSettings.local.asset.meta
